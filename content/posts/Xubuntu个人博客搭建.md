---
title: "Xubuntu 环境下 Hugo 博客搭建全指南：从下载到云端发布"
date: 2023-10-27T20:00:00+08:00
draft: false
tags: ["Xubuntu", "Hugo", "Linux", "Guide"]
categories: ["技术教程"]
---

在 Xubuntu 这种简洁高效的 Linux 环境下，使用 Hugo 搭建静态博客是一件非常丝滑的事情。本文将带你从安装 Hugo Extended 版开始，一步步完成本地建站、主题配置及自动化部署。

## 第一阶段：环境安装 (Xubuntu 终端)

在 Xubuntu 中，我们推荐安装 **Hugo Extended** 版本，因为大多数现代主题需要它来处理 SCSS。

### 1. 安装 Git
打开 Xfce 终端（`Ctrl + Alt + T`），执行：
```bash
sudo apt update
sudo apt install git -y
```

### 2. 安装 Hugo Extended
虽然可以使用 `apt` 安装，但版本往往较旧。推荐通过 GitHub 下载最新的 `.deb` 安装包：

1.  访问 [Hugo Releases](https://github.com/gohugoio/hugo/releases/latest)。
2.  下载名为 `hugo_extended_X.X.X_linux-amd64.deb` 的文件。
3.  在下载文件夹打开终端，运行：
    ```bash
    sudo dpkg -i hugo_extended_*.deb
    ```
4.  **验证版本：**
    ```bash
    hugo version
    # 确保输出中包含 "extended" 字样
    ```

---

## 第二阶段：初始化项目

在你的家目录（Home）或工作目录下创建项目：

```bash
# 1. 创建站点文件夹
hugo new site my-cool-blog
cd my-cool-blog

# 2. 初始化 Git 仓库
git init
```

---

## 第三阶段：安装 PaperMod 主题

我们使用 Git 子模块来安装主题，这样方便以后更新。

```bash
git submodule add --depth=1 https://github.com/adityatelange/hugo-PaperMod.git themes/PaperMod

# 修改配置文件，指定主题
echo 'theme = "PaperMod"' >> hugo.toml
```

---

## 第四阶段：创作首篇文章

Hugo 会在 `content/posts` 目录下自动生成 Markdown 模板。

```bash
hugo new posts/first-post.md
```

使用 Xubuntu 自带的 **Mousepad** 或你喜欢的编辑器（如 VS Code）编辑 `content/posts/first-post.md`：

```markdown
---
title: "我的第一篇博客"
date: 2023-10-27T20:00:00+08:00
draft: false
---
你好，这是我在 Xubuntu 上发布的第一篇文章！

### 代码测试
```bash
echo "Hello Hugo from Xubuntu"
```
```

---

## 第五阶段：本地预览

在终端运行以下命令：
```bash
hugo server -D
```
打开 Firefox 浏览器，访问 `http://localhost:1313`。你会看到实时更新的网页。

---

## 第六阶段：托管至 GitHub 与 Vercel

### 1. 推送到 GitHub
在 GitHub 创建一个名为 `my-cool-blog` 的仓库，然后在本地终端运行：

```bash
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/my-cool-blog.git
git push -u origin main
```

### 2. 在 Vercel 部署
1.  登录 [Vercel](https://vercel.com/)。
2.  导入 GitHub 上的 `my-cool-blog` 仓库。
3.  Vercel 会自动识别 Hugo 框架，直接点击 **Deploy**。
4.  几秒钟后，你的博客就拥有了一个全球可访问的 HTTPS 域名。

---

## 💡 Xubuntu 用户专属小贴士

1.  **权限问题**：如果你在执行 `hugo` 命令时遇到权限报错，请确保项目文件夹归属于当前用户，而不是 root。可以使用 `sudo chown -R $USER:$USER .` 修复。
2.  **快捷命令**：你可以在 `~/.bashrc` 中设置别名：
    `alias h-serve='hugo server -D --disableFastRender'`
3.  **防止代码块溢出**：在写 Markdown 时，请务必检查**三个反引号**（` ``` `）是否成对出现。如果漏掉结尾的引号，会导致整个页面进入“全代码模式”。

---

本博客由 Hugo 驱动，在 Xubuntu 的极简主义中诞生。
```

### 如何在你的新博客中使用此内容？

1.  按照上述“第二阶段”和“第三阶段”建好站。
2.  执行 `hugo new posts/hugo-guide.md`。
3.  用文本编辑器打开该文件，将上面框框里的内容**全部覆盖粘贴**进去。
4.  运行 `hugo server` 预览效果。
